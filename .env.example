VITE_CARTO_API_KEY=YOUR_KEY
# Base URL of the deployed Nirvasan API (from `sam deploy` output). Leave empty to use bundled demo data.
VITE_API_BASE_URL=
# Cognito ids from the `sam deploy` outputs. Leave empty to keep the demo login form.
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
